// Types for Prisma models
import { HabitType, Period, Unit, Visibility, Source } from '@prisma/client'

// If you need to extend or create new types based on Prisma enums or models,
// you can do so here. For now, we'll just re-export the Prisma types.
// This file can be expanded as needed.

export type { HabitType, Period, Unit, Visibility, Source }